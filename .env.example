# Server Configuration
PORT=8080

# You can override the port with any value you want
# The application will use process.env.PORT if available (for deployment platforms)
# or fall back to 8080 for local development
